import { CSSProperties } from "react";
import { theme } from "../../../theme/constants";

const container: CSSProperties = {
  display: "flex",
  flexDirection: "row",
  gap: theme.gap,
};

export const styles = {
  container,
};
